packages:
  - 'apps/*'

# pnpm v10+ reads settings (including overrides) from here instead of the
# "pnpm" field in package.json.
overrides:
  '@apollo/client': 3.8.0
  '@radix-ui/react-dialog': 1.1.1
  prisma: ^5.22.0
